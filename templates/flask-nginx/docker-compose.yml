services:
  app:
    build: .
    env_file: .env
    expose: ["8000"]
  nginx:
    image: nginx:alpine
    depends_on: [app]
    ports: ["8080:80"]
    volumes:
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./static:/usr/share/nginx/html/static:ro
